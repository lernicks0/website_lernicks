# 全站提速更新（2026-09-09）

覆盖主站、便签、班级中心、擂台、积分、目标、文档、图床、新闻及共享账号组件。

## 改动

- 公开入口由 `site-performance/static.cjs` 提供：按文件修改时间复用内容、预压缩 Gzip、ETag/304，HEAD 不传正文。只处理固定白名单，账号数据、新闻审核、成绩、文档正文、文件上传下载仍交回原服务。
- 白名单中的 CSS、JS、图片在 HTML 响应中自动附带内容版本号；版本匹配才启用一年缓存。HTML 与无版本资源每次校验，源码不被重写，更新组件时自动失效。
- 对现有 Nginx 中本项目域名添加文本/API 压缩；已有 server 层 gzip 设置不覆盖，不改 TLS、反向代理或 Cloudflare。没有新增任何接口公共缓存。
- 积分、目标、擂台消除首次账号事件与 ready 重复加载；共享账号组件合并同时发生的会话刷新，账号服务连接池复用，同一 HTTP 请求内重复鉴权只查询一次。不同请求重新验证，不缓存权限，不推迟退出或重置密码生效。
- 新闻首页/文章和账号查询并行；初始账号事件不重复下载新闻。新闻数据按文件版本复用，成功写入立即失效，外部修改可被识别。
- 未改图片原文件或图床上传质量。当前主页面主要是内联样式和脚本；图床原有文件缓存已启用。未盲目迁移数据库或购买服务器。

## 验证结果

`node tests/performance.cjs` 测试压缩解压一致、304、版本失效、HEAD、私密路径、Nginx 范围隔离、会话请求合并及撤销、慢账号接口下新闻首屏不阻塞。
`node tests/news.integration.cjs` 和 `node tests/teacher-role.integration.js` 已通过。
本地文件压缩测量：主要页面减少 73%～75%，新闻脚本减少 63%。这是传输体积，不是公网实际加载耗时。测量见 `deploy/performance-measurements.json`。

## 安装原则

安装器读取服务器当前页面和代码，按上下文应用局部补丁，不整站覆盖；保留后续老师账号、弹窗配色和关闭按钮修复。无法匹配补丁时退出，不猜测替换。每次安装备份到 `/root/performance-backup-*`。
保留所有账号、私密名单、密码、会话、新闻、成绩、文档、上传和便签数据。检查 Nginx 后重启涉及的 PM2 服务并验证源站压缩/缓存，失败恢复原程序和配置。
默认端口沿用各站部署文档；如果服务器自行改过端口，先调整安装验证端口表。
没有实际执行服务器安装并验证公网前，不宣称线上部署完成。
