# 扣分原因（2026-09-11）

积分站扣分可填写不超过 200 字的原因，或点选“自习课讲话/打闹”“打闹”。省略、空文字或纯空格统一使用“自习课讲话/打闹”。适用于 −1 和自定义负整数；加分不记扣分原因。服务端补默认值，兼容旧页面提交。

每笔扣分的学生、分数、原因、时间持久保存在原积分数据文件的 deductions 数组中，登录后在同学弹窗查看。旧数据自动兼容，不改原积分和归档，不为过去的扣分编造原因。恢复归档保留扣分历史，历史记录不代表当前积分的完整流水。原权限不变；数据文件禁止直接静态下载。

仅发布 `sco/server.js`、`sco/index.html`（本地目录 sco-site），不包含私密名单或积分数据。脚本 `deploy/install-score-reasons-20260911.sh` 备份并更新 `/root/sco/` 中两个程序文件，重启 `sco-server` 后验证页面和登录要求，失败恢复程序。兼容已安装或未安装提速组件的服务器。

将脚本按固定 GitHub 提交下载，校验 SHA-256 后以提交 ID 为参数执行。成功标志 `SCORE_REASONS_SUCCESS`，随后刷新网页。GitHub 发布不代表服务器已安装。

验证：`node tests/score-reasons.cjs` 覆盖默认、预设、自定义、输入校验、权限、并发扣分、保存读取、归档保留和禁止下载数据文件；设置 PLAYWRIGHT_MODULE 时进一步验证真实页面按钮、负数提交、原因安全显示、深浅色手机布局和刷新。另回归 `node tests/teacher-role.integration.js`。
