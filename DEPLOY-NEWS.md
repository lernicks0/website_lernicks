# 803 新闻站（2026-09-08）

- 计划域名：`https://news.lernicks.cn`，进程 `news-server`，仅监听 `127.0.0.1:1154`。
- 本地程序：`news-site/`；仓库和服务器程序目录分别为 `news/`、`/root/news/`。
- 使用现有 `/root/class-auth/client.js` 及 1153 账号服务，不更改账号系统和私密名单。
- 新闻持久化文件：`/root/news/data/news.json`。只更新四个程序文件，永远保留 data 目录。
- 审核身份由账号服务返回的真实姓名“王韩润”识别；其他学生管理员、老师均不能审核。可由服务器环境变量 `NEWS_REVIEWER_ID` 固定为名单中的账号 ID，不在公开文件中写入学号。
- 四个分区：运动会预选赛、日常生活、八卦、专题（用户文字写“两个”但具体列出了四项，按列举实现）。
- 浏览已发布文章无需登录；登录后可实名或匿名投稿；待审稿件只对本人和审核人可见。实名显示账号姓名，匿名公开接口不返回姓名或学号，审核人可查看真实投稿人。
- 投稿会持久化送入王韩润的站内审核箱，导航显示待审数，每 30 秒更新；没有接入短信、邮件、微信等外部通知。
- 审核通过后上首页；退回时必须填写意见，投稿人可以修改后重新投稿。已发布内容不可直接修改，避免绕过审核。
- 引题和副标题可选，可使用预设、鼠标/触屏拖动或方向键调整，按比例保存位置，阅读页跟随容器尺寸排版。正文按纯文本分段显示，不执行投稿中的 HTML。
- CLA 仅补丁添加顶部“班级新闻”链接，不替换服务器现有页面或改变三个工具入口布局。

## 发布与安装

更新包 `deploy/news-20260908.tar.gz` 仅包含 news 的四个程序文件、CLA 入口补丁及 Nginx 示例。
安装脚本 `deploy/install-news-20260908.sh` 使用固定 Git 提交下载包并校验 SHA-256；先确认私密名单和账号服务，再备份程序、启动服务并验证本机接口与域名转发。安装失败会恢复程序，新闻数据不回滚。

Cloudflare DNS 需要代理 A 记录 `news` → `124.223.201.40`。Nginx 示例延续历史 HTTP 源站代理配置；若源站要求 HTTPS，须接入现有源站证书和 443 配置。

没有实际在服务器安装并检查公网结果前，不能写“线上部署完成”。发布提交和 WebShell 命令在本次任务及 `deploy/news-published.txt` 记录。

## 验证

运行 `node tests/news.integration.cjs`：真实临时账号服务验证共享登录、指定审核人、匿名接口脱敏、待审隔离、退回、类别筛选、输入校验、跨站请求拒绝、数据路径保护、并发写入、重启持久化和 CLA 补丁幂等。
运行 `node --check news-site/server.js` 与 `node --check news-site/app.js` 检查语法。

## 本地启动

已有本地账号服务时运行 `node news-site/server.js`，访问 `http://127.0.0.1:1154`。没有账号服务时可浏览公开首页，但不能登录或提交；不要在公开更新包里放入测试账号或假新闻。
